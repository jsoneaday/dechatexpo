import React, {useRef, useState} from 'react';
import {
  GestureResponderEvent,
  Platform,
  StyleSheet,
  FlatList,
  View,
} from 'react-native';

export default function PostedMessageProgressBar() {
  return <></>;
}
